/*
    11. Triangle Validator
    Take three angles a, b, and c.

    Determine whether they can form a triangle.

    A triangle is valid if:

    a + b + c == 180

    and all three angles are greater than 0.
*/
#include<bits/stdc++.h>
using namespace std;
